import globals from "globals";


export default [
  {
    files: ['*.js'],
    languageOptions: { globals: globals.node },
    plugins: {
      // Добавь нужные плагины
    },
    rules: {
      // Настрой свои правила
    },
  },
];
